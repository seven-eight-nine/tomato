namespace Tomato.ReconciliationSystem;

/// <summary>
/// Entity種別。
/// 押し出し優先度の判定に使用される。
/// </summary>
public enum EntityType
{
    Player,
    Enemy,
    NPC,
    Obstacle,
    Wall,
    Projectile
}
